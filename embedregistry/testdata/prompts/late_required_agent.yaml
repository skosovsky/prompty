id: late_required_agent
inputs:
  user_query:
    type: string
    required: true
  patient_dossier:
    type: string
    late: true
    required: true
messages:
  - role: user
    content: "{{ .Input.user_query }} {{ .LateVars.patient_dossier }}"
